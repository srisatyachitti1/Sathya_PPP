# there are two types of files, Text files and binary files. In text files we may have
#plaintext, xml, json, source code. In Binary files, we may have Compiled code,
# App data, Media files like images, audio, video. This example is for Text files
#character meaning r - read, w - writing, truncating the file first
#x - create a new file and write, a- open for write, append at the end of file if file exists
#b- binary mode, t- text mode, + open a disk file for updating, reading and writing
# U - universal new line mode - deprecated
